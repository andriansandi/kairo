import { PlanningInput, Conflict } from "@kairo/types";

// TODO(blueprint §8): implement rules C1–C10 with severity + templates.
export function evaluateConflicts(_input: PlanningInput): Conflict[] {
  throw new Error("Phase 0 stub: not implemented");
}
